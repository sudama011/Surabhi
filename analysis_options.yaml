# Dart & Flutter Static Analysis Configuration
# Configures the analyzer for error detection, warnings, and lints
# Run: flutter analyze

include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Enforce consistent code style
    prefer_single_quotes: true
    prefer_const_constructors: false  # Too strict for this project
    prefer_const_literals_to_create_immutables: false  # Too strict

    # Improve code quality
    avoid_print: false  # Allow print statements for debugging
    avoid_unnecessary_containers: true
    avoid_web_libraries_in_flutter: true

    # Documentation
    public_member_api_docs: false  # Disable for internal project

analyzer:
  # Exclude generated files from analysis
  exclude:
    - lib/**.g.dart
    - lib/**.freezed.dart
    - build/**
    - android/**
    - ios/**
    - web/**

  # Disable strict checking for this project
  language:
    strict-casts: false
    strict-inference: false
    strict-raw-types: false

formatter:
  page_width: 120
